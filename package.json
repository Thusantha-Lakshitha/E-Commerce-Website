{
  "name": "online-shopping-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server --legacy-peer-deps && npm install --prefix client --legacy-peer-deps",
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client"
  }
}
